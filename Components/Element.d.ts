import { PropsWithChildren } from 'react';
import { ElementProps } from '../types';
declare const Element: ({ type, duration, direction, iteration, timing, fillMode, delay, children, }: PropsWithChildren<ElementProps>) => JSX.Element;
export default Element;
